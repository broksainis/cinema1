html
  head
    title= title
  body
    h1= title
    ul
        each movie in movies
            li
                b= movie.OriginalTitle
            ul
                each event in movie.Events
                    li= `${new Date(event.dttmShowStart).toLocaleDateString("en-US")} - ${event.Theatre}`
